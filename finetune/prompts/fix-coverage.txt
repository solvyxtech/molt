Defect: the work-proven check (diff-covered builtin, src/bar.ts) passes on every receipt in this project because coverage/lcov.info names files as dist-test/src/*.js with compiled line numbers while the write ledger names src/*.ts with source line numbers; coverageFor in src/coverage.ts matches by path suffix, so bar.js never matches bar.ts. Input: any changed line in src/bar.ts. Wrong output: "0 changed file(s) executed by the tests · 1 not in the coverage report". Right output: a changed line that no test executes fails as "never executed". First write a test that fails today and demonstrates the defect, then fix the cause so the test passes. Do not delete or invert any existing assertion. Quote verbatim the line of src/coverage.ts you judge against.
