Read docs/testing-charter.md section F. Then audit .molt/receipts/index.jsonl against the receipt files on disk in .molt/receipts/, using only list_dir, read_file and grep. Report every place where the index and the files disagree: rows whose file is missing, files with no row, and two rows that name the same file. For each, give the input you looked at, the wrong output, the right output, and one shell command that reproduces it. Change no files.
